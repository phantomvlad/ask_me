<div class="container">
  <% if @question.errors.any? %>
    <div class="mt">
      <% @question.errors.full_messages.each do |message| %>
        <p class="mb-sm text-red"><%= message %></p>
      <% end %>
    </div>
  <% end %>
  <h2 class="title-lg mb-sm">Новый вопрос для <%= link_to "@#{@question.user.nickname}", user_path(@question.user) %></h2>

  <%= render 'form', question: @question %>
</div>
